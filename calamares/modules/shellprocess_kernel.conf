# Restores the kernel that mkarchiso deleted from /boot when it packaged the
# image. Runs in the target: the kernel comes from the target's own
# /usr/lib/modules, so nothing outside it is needed.
---
dontChroot: false
timeout: 300
script:
    - "/usr/local/lib/starch/copy-kernel /"
