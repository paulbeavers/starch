# Copy the live filesystem onto the target. The source is the squashfs archiso
# already has mounted at /run/archiso/airootfs — the same image the live session
# is running from, so what gets installed is exactly what was tested.
---
unpack:
    - source: /run/archiso/airootfs
      sourcefs: squashfs
      destination: ""
