# A copied filesystem carries the live image's identity. Everything here has to
# be regenerated or the installed machine is a clone of the ISO.
---
systemd: true
dbus: true
dbus-symlink: true
entropy-files: []
