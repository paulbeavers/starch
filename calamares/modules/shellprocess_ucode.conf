# Between the step that writes mkinitcpio.conf and the one that builds the
# images. Earlier would be overwritten by initcpiocfg; later would be too late
# for initcpio to act on.
---
dontChroot: false
timeout: 120
script:
    - "/usr/local/lib/starch/add-microcode"
