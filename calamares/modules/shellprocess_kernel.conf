# Runs in the live system, not the target: the kernel it copies lives under
# /run/archiso, which the chroot cannot see.
---
dontChroot: true
timeout: 300
script:
    - "/usr/local/lib/starch/copy-kernel ${ROOT}"
