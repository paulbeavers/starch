# Build trees and output. mkarchiso's work dir is several GB and the ISO is
# well over a gigabyte; neither belongs in the repository.
/work/
/work-assemble/
/out/
/.vm/
# Built by tools/build-aur.sh — packages and a repo database, not source.
/localrepo/
/.build-aur/
/.build-calamares/
