# Between the step that writes mkinitcpio.conf and the one that builds the
# images, exactly like the microcode hook and for the same reason: earlier
# would be overwritten by initcpiocfg, later would be too late for initcpio.
---
dontChroot: false
timeout: 120
script:
    - "/usr/local/lib/starch/add-plymouth"
